using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio6_2 : MonoBehaviour
{
    //A) W || Y && X && W || Z = W ||true && W || Z = W || true && W || Z = W || false || Z = false || Z = false
    //B) X && !Y && !X || !W && Y = false && !X || !W && Y = true || true = true
    //C) !(W || !Y) && X || Z = true && X || Z = true || Z = true
    //D) X && Y && W || Z || X = true && W || Z || X = false || Z || X = false || X = true


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
