using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio6_1 : MonoBehaviour
{
    //A) (X && Y) || (X && Z) = false || true = true
    //B) (X || !Y) && (!X || Z) = true && true = true
    //C) X || Y && Z = X || false = true
    //D) !(X || Y) && Z = false && Z = false
    //E) X || Y || X && !Z && !Y = X || Y || false && !Y = X || Y || false = true || false = true
    //F) !X || !Y || Z && X && !Y = !X || !Y || true = true || true = true

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
